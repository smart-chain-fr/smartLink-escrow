import Vue from 'vue';
import Vuetify from 'vuetify/lib';

Vue.use(Vuetify);

export default new Vuetify({
    theme: { themes:{ light: { background : '#f8f9fd', primary: '#5a6099', forward: '#7128fc', back:'#454052', 'v-divider': '#ffffff6e'}} },
  })